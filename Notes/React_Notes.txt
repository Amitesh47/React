*ELEMENTS:
    2 ways : i) using React.createElement(element,attributes,children)
             ii) using jsx(not the best practises)

*COMPONENTS: 
        - render() method is a mandatory method which renders the element that the component will return.

*PROPS: 
        -props are read only entity

*STATE: 
        -states are immutable entity

*REF:
        - 'ref' is used as id in react

*LIFE CYCLE HOOKS:
        -componentWillMount
        -componentDidMount
        -componentWillReceiveProps
        -shouldComponentUpdate
        -componentWillUpdate
        -componentDidUpdate
        -componentwillUnmount

*GITHUB LINK: https://github.com/SumeetWajpe/Sapient_ID_102_April_20

*LINK:    https://api.myjson.com/bins/rwxws

*FUNCTIONAL COMPONENTS:
        -also called as Stateless Components.
        -used when we dont require a state.
        -doesnt have life cycle hooks.
        -doesnt have a render method.

*ROUTING:
        -


      